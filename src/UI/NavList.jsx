import styled from 'styled-components';

export const NavList = styled.ul`
  display: flex;
  justify-content: space-around;
  width: 40vw;
  list-style: none;
  font-weight: 900;

  font-size: 1.1rem;
`;
