import styled from 'styled-components';

export const PlayerWrapper = styled.section`
  button {
    position: absolute;
  }
  &.react-player {
    position: absolute;
    top: 0;
    left: 0;
    opacity: 1;
  }
`;
