import styled from 'styled-components';

export const ReactPng = styled.img`
  filter: grayscale(75%);
  opacity: 0.6;
  width: 3rem;
  filter: grayscale(75%) drop-shadow(0 0 8px rgb(220, 220, 220));
`;
