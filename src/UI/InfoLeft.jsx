import styled from 'styled-components';

export const InfoLeft = styled.div`
  position: fixed;
  cursor: pointer;
  z-index: 10;
  align-items: center;
  gap: 1rem;
  left: 1rem;
  bottom: 0;
  display: flex;
  flex-direction: column;
`;
